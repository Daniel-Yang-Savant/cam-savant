# Vercel 環境變數「Needs Attention」處置清單

> 背景：Vercel 將仍以明文（非 Sensitive）儲存、可被讀取的密鑰標記為「Needs Attention」（與 2026/04 環境變數外洩事件有關）。
> 處置原則：**輪替新值 → 在 Vercel 重新新增並勾選 Sensitive → 重新部署**。
> 程式碼不需改動（皆從 `process.env` 讀取；Sensitive 變數在 build 與 runtime 都可讀）。
> ⚠️ 所有新值請本人在來源端產生與填寫。

---

## 共通操作（每個變數都一樣）

1. 先在來源端取得／設定新值。
2. Vercel → 專案 → Settings →（左欄）Environment Variables。
3. 找到該變數 → 右邊 **⋮ → Remove**（刪掉舊的明文版本）。
4. **Add New**：
   - Key：同樣的名稱
   - Value：貼上新值
   - 勾選 **Sensitive**
   - Environments：All Environments
   - Save
5. 全部改完後 → Deployments → 最新一筆 **⋮ → Redeploy**（讓新值生效）。

---

## 逐項處置（依風險高低）

### 1. ANTHROPIC_API_KEY　★最優先（真正的外部金鑰）
> ⭐ 已決定移除衛教單張生成器（見最下方「附錄」）。移除後，**正式環境不再用到這把金鑰**，
> 建議直接從 Vercel 移除（不必輪替成 Sensitive）。僅本機 `publish-from-pdf` 腳本會用到。
- 原用途：`/api/generate-pe`（Claude 生成衛教單張，已移除）、`scripts/publish-from-pdf.ts`（本機）。
- 處置（擇一）：
  - **A（建議）**：Vercel 移除 `ANTHROPIC_API_KEY`。若仍跑 `npm run publish-post`，把金鑰留在本機 `.env.local`；否則本機也刪。為保險仍建議到 console.anthropic.com **撤銷舊金鑰、另開一把新的**只放本機。
  - **B**：若暫時保留線上功能，才需輪替＋標 Sensitive。

### 2. ADMIN_SECRET　★管理員登入密碼
- 用途：`middleware.ts`、`/api/admin-login`、`/api/generate-pe`。同時是 `/admin-login` 密碼與 `admin_token` cookie 值。
- 輪替：自訂一組新的高強度隨機字串（建議 32+ 字元）。
- 更新：Vercel 重加為 Sensitive；本機 `.env.local` 同步。
- ⚠️ 重新部署後需用**新密碼**重新登入後台，舊登入狀態失效。

### 3. PERIOP_ACCESS_TOKEN　術後復健存取碼（風險最低）
- 用途：`middleware.ts`、`/perioperative-rehab/access`。控制術後復健頁的存取。
- 選項 A（建議，要 Sensitive）：手動設新值 → Vercel 重加為 Sensitive。
- 選項 B（方便）：`npm run rotate-token` 自動產生新碼＋更新 Vercel＋重部署＋印新 QR；但會寫成「非敏感」，之後仍會被標記。
- 二擇一即可，別兩者混用。

---

## 完成後確認

- [ ] 三個變數都已重新以 **Sensitive** 新增，舊明文版本已刪除
- [ ] ANTHROPIC 舊金鑰已在 Anthropic Console 撤銷
- [ ] 已 Redeploy，且 `/admin-login` 能用新 ADMIN_SECRET 登入
- [ ] Vercel 環境變數頁不再顯示「Needs Attention」

> 備註：頁面下方「Enable access to System Environment Variables」維持勾選即可，與此問題無關。

---

## 附錄：移除「衛教單張生成器」（降低 ANTHROPIC_API_KEY 風險）

未使用此功能，移除可讓正式環境完全不需要 ANTHROPIC_API_KEY。

**已由 Claude 完成（程式端）**
- [x] `middleware.ts` 移除 `/perioperative-rehab/pe-generator` 的權限區塊（`/admin` 編輯器保護保留）
- [x] `CLAUDE.md` 更新（標註此功能已移除）

**你要在電腦上做**
1. 刪除兩個檔案：
   ```
   cd ~/Desktop/cam-savant
   git rm app/api/generate-pe/route.ts app/perioperative-rehab/pe-generator/page.tsx
   ```
2. Vercel 移除 `ANTHROPIC_API_KEY`（Settings → Environment Variables → ⋮ → Remove）。
3. commit + push：
   ```
   rm -f .git/index.lock .git/HEAD.lock && rm -f dryang-v3.png
   git add -A
   git commit -m "chore: 移除未使用的衛教單張生成器、清理 ANTHROPIC_API_KEY 依賴"
   git push
   ```

> 保留事項：`@anthropic-ai/sdk` 套件仍被本機 `scripts/publish-from-pdf.ts` 使用，**不要**從 package.json 移除。
> `ADMIN_SECRET`（文章編輯器）與 `PERIOP_ACCESS_TOKEN` 仍需保留，依上方步驟輪替／標 Sensitive。
