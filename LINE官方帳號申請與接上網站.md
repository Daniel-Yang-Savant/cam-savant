# LINE 官方帳號申請 → 接上網站　操作指南

> 目標：申請一個 LINE 官方帳號（LINE Official Account, 簡稱 LINE OA），拿到「加好友連結」與「官方帳號 ID」，填回網站即可全站上線。
> 網站端的版位（浮動按鈕、Footer、看診資訊頁）已經做好，**只差你的連結**。

---

## 一、申請 LINE 官方帳號（約 10 分鐘）

### 1. 進入申請頁
電腦開啟 LINE Official Account Manager：**https://manager.line.biz**
用你的**個人 LINE 帳號**登入（或 LINE Business ID）。

### 2. 建立帳號
點「建立帳號（Create an account）」，填寫：

- **帳號名稱**：建議用「楊育愷醫師」或「楊育愷醫師 · 復健 / 運動醫學」
  （名稱之後可改一次，取一個病人搜尋得到的名字）
- **大頭貼**：用你的醫師照（網站 `/public/images/team/yu-kai-yang.jpg` 那張就很適合）
- **產業類別**：選「醫療保健 → 醫療院所 / 診所」
- **帳號類型**：先選「一般帳號（未認證）」即可，免費、立即可用

完成後就會進到管理後台。

### 3.（選擇性，但建議）申請「認證帳號」
未認證帳號的 ID 是一串亂碼（例如 `@123abcd`），認證後可換成好記的 ID 並出現藍盾。

- 後台 → 設定 → 帳號管理 → 申請認證
- 需要填寫醫療機構 / 執業相關資料，審核約 5–10 個工作天
- **不急的話可以先用未認證帳號上線，認證通過後再回來更新 ID 即可**

---

## 二、取得要填回網站的兩個值

進入後台後，找這兩樣東西：

### ① 加好友連結（最重要）
後台 → 「主頁網址 / 加入好友（Add friends）」→ 會看到一個短網址，形如：

```
https://lin.ee/AbCdEfG
```

把它複製下來。這就是 `LINE_ADD_URL`。

### ② 官方帳號 ID
後台 → 設定 → 帳號設定，會看到 **基本 ID**（例如 `@abc1234`，**一定含 @**）。
這就是 `LINE_ID`。

> 認證帳號才能把 `@abc1234` 換成自訂的好記 ID（例如 `@dryang`）。

---

## 三、填回網站（兩種方式擇一）

### 方式 A：直接改一個檔案（最簡單）
打開 `lib/site.ts`，把這兩行的預設值換成你的：

```ts
export const LINE_ADD_URL =
  process.env.NEXT_PUBLIC_LINE_URL || 'https://lin.ee/AbCdEfG'   // ← 換成你的加好友連結

export const LINE_ID =
  process.env.NEXT_PUBLIC_LINE_ID || '@abc1234'                  // ← 換成你的 LINE ID
```

存檔 → `git commit` → push，Vercel 會自動重新部署。

### 方式 B：用環境變數（不想動程式碼時）
Vercel → 你的專案 → Settings → Environment Variables，新增：

- `NEXT_PUBLIC_LINE_URL` = `https://lin.ee/AbCdEfG`
- `NEXT_PUBLIC_LINE_ID` = `@abc1234`

存檔後 → Deployments → Redeploy。
（本機測試的話，把同樣兩行加到 `.env.local`。）

> 程式已寫成：**有環境變數就優先用環境變數，否則用 `lib/site.ts` 裡的值**。兩種方式都可以，不會衝突。

---

## 四、上線後會看到的效果

- **右下角浮動按鈕**：點「💬 諮詢提問」會展開兩個選項 → 「官方 LINE 諮詢」與「📝 表單提問」
- **Footer**：每頁底部都有「加入好友」綠色按鈕與 LINE ID
- **看診資訊頁（/contact）**：桌機顯示 QR code 供掃描、手機顯示「加入好友」按鈕

QR code 是依你的加好友連結**自動生成**的，連結填對就會自動正確，不用另外做圖。

---

## 五、申請完之後，建議在 LINE 後台做的事

1. **加入歡迎訊息**：新好友加入時自動回覆（自我介紹 + 門診時間 + 提醒「LINE 不做緊急醫療判斷」）
2. **設定圖文選單（Rich Menu）**：放「門診時間」「線上掛號」「衛教文章」「諮詢」四格
3. **分眾標籤**：之後可依運動傷害 / 復健 / 自費療程分眾推播

---

## ⚠️ 合規提醒（自費療程相關）

LINE OA 與網站都涉及醫療資訊，內容請留意《醫療法》醫療廣告規範：

- 避免「療效保證」字眼（如「保證治好」「100% 有效」）
- 避免使用病人見證 / 前後對比照作為招攬
- 自費項目（增生療法、PRP、FSM）的說明以衛教與實證為主，不誇大
- LINE 諮詢回覆建議加上「線上資訊不取代面對面診療」的提醒

---

申請過程中任何一步卡住，把後台畫面截圖給我，我可以指你下一步該點哪裡。
